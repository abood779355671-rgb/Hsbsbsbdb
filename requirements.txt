pyrogram==2.0.106
tgcrypto>=1.2.5
redis[asyncio]>=5.0.0
pytz>=2024.1
httpx[http2]>=0.27.0
aiohttp>=3.9.0
meval>=0.0.6
psutil>=5.9.0
py-cpuinfo>=9.0.0
pytio
# ✅ إصلاح 6: رفع yt-dlp — يوتيوب غيّر بروتوكوله والإصدار القديم لا يعمل
yt-dlp>=2025.1.0

# ✅ إصلاح 6: رفع shazamio — 0.4.0 فيه bugs في التعرف على الصوت
shazamio==0.5.1

# ─── ملاحظات ───────────────────────────────────────────────────────────
# ✅ numpy: حُذفت — غير مستخدمة في أي ملف، تُضيف ~50MB وتُبطئ الإقلاع
# ✅ Pillow: حُذفت — غير مستخدمة في أي ملف
# ✅ requests: حُذفت سابقاً — المشروع يستخدم httpx
# ✅ wget: حُذف سابقاً — yt-dlp وhttpx يغطيانه
# ✅ h2: مدمج في httpx[http2]
# ✅ pytio: حُذف سابقاً — غير موجود على PyPI
